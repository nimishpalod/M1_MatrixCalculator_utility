name: C/C++ CI

on:
  push:
    branches: [ nimish ]
  pull_request:
    branches: [ nimish ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: gcc matrix_cal.c -o matrix_cal.exe
    - name: Run
      run: ./matrix_cal.exe
